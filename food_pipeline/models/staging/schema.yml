version: 2

models:
  - name: stg_weather
    columns:
      - name: weather_date
        tests:
          - not_null
          - unique

  - name: stg_food_prices
    columns:
      - name: price_month
        tests:
          - not_null
          - unique
